import { Router } from "express";
import { sql } from "drizzle-orm";
import { db } from "../db/client.js";
export const socialProfileLiveRouter=Router();type Row=Record<string,any>;
socialProfileLiveRouter.get("/users/:username/profile-live",async(req,res)=>{if(!db)return res.status(404).json({error:"Profile not found"});try{const[user]=(await db.execute(sql`SELECT id,name,username,email,avatar_url,bio,location,account_type,interests,trust_score,created_at,cover_url,profile_logo_url,cover_position_x,cover_position_y FROM users WHERE lower(username)=lower(${req.params.username}) LIMIT 1`) as unknown as Row[]);if(!user)return res.status(404).json({error:"Profile not found"});const[stats]=(await db.execute(sql`SELECT (SELECT COUNT(*)::int FROM follows WHERE following_id=${user.id}) followers,(SELECT COUNT(*)::int FROM follows WHERE follower_id=${user.id}) following,(SELECT COUNT(*)::int FROM projects WHERE owner_id=${user.id} OR id IN (SELECT project_id FROM project_collaborators WHERE user_id=${user.id} AND status='accepted')) projects,(SELECT COUNT(*)::int FROM posts WHERE author_id=${user.id}) posts`) as unknown as Row[]);const projects=await db.execute(sql`SELECT id,name,slug,description,stage,github_url,created_at FROM projects WHERE owner_id=${user.id} OR id IN (SELECT project_id FROM project_collaborators WHERE user_id=${user.id} AND status='accepted') ORDER BY created_at DESC LIMIT 24`) as unknown as Row[];const posts=await db.execute(sql`SELECT p.id,p.author_id,p.body,p.created_at,p.project_id,p.quote_post_id,COUNT(DISTINCT pl.user_id)::int likes,COUNT(DISTINCT pc.id)::int comments,COUNT(DISTINCT pr.user_id)::int reposts,COUNT(DISTINCT ps.user_id)::int saves FROM posts p LEFT JOIN post_likes pl ON pl.post_id=p.id LEFT JOIN post_comments pc ON pc.post_id=p.id LEFT JOIN post_reposts pr ON pr.post_id=p.id LEFT JOIN post_saves ps ON ps.post_id=p.id WHERE p.author_id=${user.id} GROUP BY p.id ORDER BY p.created_at DESC LIMIT 50`) as unknown as Row[];const affiliations=await db.execute(sql`SELECT a.id,a.name,a.slug,a.type,a.website,a.verified,aa.role FROM agent_affiliations aa JOIN agents a ON a.id=aa.agent_id WHERE aa.user_id=${user.id} ORDER BY aa.verified_at DESC`) as unknown as Row[];return res.json({data:{user:{...user,avatarUrl:user.avatar_url,coverUrl:user.cover_url,profileLogoUrl:user.profile_logo_url,coverPositionX:Number(user.cover_position_x??50),coverPositionY:Number(user.cover_position_y??50)},stats:stats??{followers:0,following:0,projects:0,posts:0},projects,posts:posts.map(p=>({id:p.id,authorId:p.author_id,text:p.body,createdAt:p.created_at,projectId:p.project_id,quotePostId:p.quote_post_id,likes:Number(p.likes??0),comments:Number(p.comments??0),reposts:Number(p.reposts??0),saves:Number(p.saves??0)})),affiliations:affiliations.map(a=>({id:a.id,name:a.name,slug:a.slug,type:a.type,website:a.website,verified:Boolean(a.verified),role:a.role,status:"accepted"}))}})}catch(error){console.error("[SocialProfileLive] Failed to load profile:",error);return res.status(500).json({error:"Unable to load profile"})}});
